# Dativo Architecture Diagrams

Visual representations of the current architecture and proposed improvements.

---

## 1. Current System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      CLI / Orchestrator                          │
│                   (dativo run / dativo start)                    │
└─────────────┬───────────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Job Configuration Layer                      │
│                                                                  │
│  ┌──────────────┐  ┌─────────────┐  ┌──────────────┐          │
│  │ Job Config   │→ │ Connector   │→ │ Asset Schema │          │
│  │ (YAML)       │  │ Recipes     │  │ (ODCS v3.0.2)│          │
│  └──────────────┘  └─────────────┘  └──────────────┘          │
│                                                                  │
│  ┌──────────────────────────────────────────────────┐          │
│  │   Connector Registry (registry/connectors.yaml)  │          │
│  │   - Type validation                               │          │
│  │   - Engine compatibility                          │          │
│  │   - Role enforcement (source/target)              │          │
│  └──────────────────────────────────────────────────┘          │
└────────────┬─────────────────────────────────────────────────┬─┘
             │                                                 │
             ▼                                                 ▼
┌────────────────────────────┐               ┌────────────────────────────┐
│    Secret Management       │               │    State Management        │
│                            │               │                            │
│  ┌──────────────────────┐ │               │  ┌──────────────────────┐ │
│  │ • Environment Vars   │ │               │  │ • File-based State   │ │
│  │ • Filesystem         │ │               │  │ • Incremental Sync   │ │
│  │ • HashiCorp Vault    │ │               │  │ • Cursor Tracking    │ │
│  │ • AWS Secrets Mgr    │ │               │  └──────────────────────┘ │
│  │ • GCP Secret Mgr     │ │               │                            │
│  └──────────────────────┘ │               └────────────────────────────┘
└────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Extraction Layer                            │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Native     │  │   Airbyte    │  │   Custom     │         │
│  │  Extractors  │  │   Engine     │  │   Plugins    │         │
│  │              │  │              │  │              │         │
│  │ • Stripe     │  │ • Docker     │  │ • Python     │         │
│  │ • HubSpot    │  │ • Subprocess │  │ • Rust (FFI) │         │
│  │ • Postgres   │  │ • JSONL      │  │ • Dynamic    │         │
│  │ • MySQL      │  │   protocol   │  │   loading    │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │   Engine Framework (BaseEngineExtractor)               │   │
│  │   - AirbyteExtractor ✅                                │   │
│  │   - MeltanoExtractor ⚠️ (stub)                        │   │
│  │   - SingerExtractor ⚠️ (stub)                         │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────┬───────────────────────────────────────────────────┘
              │
              ▼ (batches of records as List[Dict[str, Any]])
┌─────────────────────────────────────────────────────────────────┐
│                   Validation & Transformation                    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Schema Validator (against ODCS asset definition)      │    │
│  │  - Strict mode: fail on mismatch                       │    │
│  │  - Warn mode: log warnings, continue                   │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Tag Derivation & Propagation                          │    │
│  │  - Field-level classification                          │    │
│  │  - FinOps metadata enrichment                          │    │
│  │  - Governance tag injection                            │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────┬───────────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Writing Layer                             │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Parquet    │  │   Iceberg    │  │   Custom     │         │
│  │   Writer     │  │   Committer  │  │   Writers    │         │
│  │              │  │              │  │              │         │
│  │ • PyArrow    │  │ • PyIceberg  │  │ • Python     │         │
│  │ • Batching   │  │ • Catalog    │  │ • Rust       │         │
│  │ • 128-200MB  │  │   metadata   │  │ • Format-    │         │
│  │   target     │  │ • Optional   │  │   specific   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────┬───────────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Target Storage                              │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │      S3      │  │    MinIO     │  │  Iceberg     │         │
│  │              │  │              │  │  Catalog     │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. Plugin Loading & Execution Flow

### Current: No Isolation

```
User Job Config
     │
     ▼
┌─────────────────────────────────────────┐
│  PluginLoader.load_reader()             │
│                                          │
│  1. Parse path: "plugin.py:MyReader"    │
│  2. importlib.util.spec_from_file_      │
│     location()                           │
│  3. exec_module()  ⚠️ NO ISOLATION     │
│                                          │
│  Plugin runs in SAME PROCESS:           │
│  - Full filesystem access                │
│  - All environment variables             │
│  - Can read /secrets/ directory          │
│  - No CPU/memory limits                  │
└─────────────────────────────────────────┘
         │
         ▼
    Main Process Memory
    (credentials exposed!)
```

### Proposed: Docker Sandbox

```
User Job Config
     │
     ▼
┌─────────────────────────────────────────┐
│  PluginSandbox.run_plugin()             │
│                                          │
│  1. Create temp directory               │
│  2. Copy plugin code (read-only)        │
│  3. Write config to temp file           │
│  4. Launch Docker container:            │
│     - Read-only filesystem              │
│     - Secrets as env vars only          │
│     - CPU limit: 50%                    │
│     - Memory limit: 2GB                 │
│     - Network: optional whitelist       │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│   Docker Container (ISOLATED)           │
│                                          │
│   /plugin/                               │
│   ├── plugin.py (read-only)             │
│   └── config.json (read-only)           │
│                                          │
│   Environment:                           │
│   - STRIPE_API_KEY=***                  │
│   - AWS_ACCESS_KEY=***                  │
│                                          │
│   No access to:                          │
│   - Host /secrets/ directory             │
│   - Other environment variables          │
│   - Other tenants' data                  │
└─────────────────────────────────────────┘
         │
         ▼
    Stdout/Stderr
    (JSON records)
```

---

## 3. Connector vs Plugin Decision Tree

```
                    Need Data Integration?
                           │
                           ▼
        ┌──────────────────────────────────────┐
        │ Is it a standard source/target?      │
        │ (Stripe, HubSpot, Postgres, S3)      │
        └──────────┬───────────────────┬───────┘
                   │                   │
                 YES                  NO
                   │                   │
                   ▼                   ▼
        ┌──────────────────┐  ┌────────────────────────┐
        │ Use CONNECTOR    │  │ Need 10-100x perf?     │
        │                  │  └──────┬────────────┬────┘
        │ ✅ Reusable      │        YES           NO
        │ ✅ Multi-engine  │         │            │
        │ ✅ Tenant-agnostic│        ▼            ▼
        └──────────────────┘  ┌───────────┐  ┌────────────┐
                              │   Rust    │  │   Python   │
                              │  Plugin   │  │   Plugin   │
                              │           │  │            │
                              │ ✅ Fast   │  │ ✅ Easy    │
                              │ ⚠️ Complex│  │ ✅ Rich    │
                              └───────────┘  │    libs    │
                                             └────────────┘

        Example Use Cases:
        ├── Connector: Stripe API (use Airbyte connector)
        ├── Python Plugin: Proprietary REST API with custom auth
        └── Rust Plugin: 10GB CSV files (15x faster than pandas)
```

---

## 4. Data Flow: Extract → Validate → Write

```
┌──────────────────────────────────────────────────────────────────┐
│                         Source System                             │
│              (Stripe API, Postgres, CSV file, etc.)               │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   EXTRACT       │
                    │                 │
                    │ reader.extract()│
                    │                 │
                    │ Yields batches: │
                    │ List[Dict]      │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   VALIDATE      │
                    │                 │
                    │ Against ODCS    │
                    │ asset schema:   │
                    │                 │
                    │ • Type check    │
                    │ • Required      │
                    │   fields        │
                    │ • Constraints   │
                    └────────┬────────┘
                             │
                   ┌─────────┴─────────┐
                   │                   │
                STRICT               WARN
                 MODE                MODE
                   │                   │
                   ▼                   ▼
            ┌──────────┐      ┌────────────┐
            │  Fail on │      │ Log warning│
            │ mismatch │      │ & continue │
            └─────┬────┘      └──────┬─────┘
                  │                  │
                  └─────────┬────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │  ENRICH TAGS    │
                   │                 │
                   │ • Classification│
                   │ • FinOps tags   │
                   │ • Team owner    │
                   │ • Compliance    │
                   └────────┬────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │   WRITE         │
                   │                 │
                   │ writer.write_   │
                   │   batch()       │
                   │                 │
                   │ • Parquet       │
                   │ • 128-200 MB    │
                   │ • Partitioned   │
                   └────────┬────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │   COMMIT        │
                   │                 │
                   │ writer.commit_  │
                   │   files()       │
                   │                 │
                   │ Optional:       │
                   │ • Iceberg       │
                   │   catalog       │
                   │ • Finalize txn  │
                   └────────┬────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────────┐
│                    Target Storage (S3/MinIO)                    │
│                                                                 │
│  raw/stripe/customers/                                          │
│  ├── part-00001.parquet (150 MB)                               │
│  ├── part-00002.parquet (145 MB)                               │
│  └── part-00003.parquet (180 MB)                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. Registry-Driven Validation

```
User submits job: "stripe → iceberg"
         │
         ▼
┌────────────────────────────────────────────┐
│  ConnectorValidator.validate_job()         │
│                                             │
│  1. Load registry/connectors.yaml          │
│                                             │
│  2. Check source type "stripe":            │
│     ✅ Exists in registry                  │
│     ✅ roles: [source]                     │
│     ✅ engines_supported: [airbyte, native]│
│     ✅ allowed_in_cloud: true              │
│                                             │
│  3. Check target type "iceberg":           │
│     ✅ Exists in registry                  │
│     ✅ roles: [target]                     │
│     ✅ file_formats: [parquet]             │
│                                             │
│  4. Validate incremental strategy:         │
│     ✅ supports_incremental: true          │
│     ✅ strategy matches default: "created" │
│     ✅ cursor_field present                │
└────────────────────────────────────────────┘
         │
         ▼
    Job validated ✅
    Proceed to execution
```

---

## 6. Multi-Engine Support Matrix

```
                    User Job Config
                          │
                          ▼
            ┌─────────────────────────┐
            │  Connector Recipe       │
            │                         │
            │  default_engine:        │
            │    type: airbyte        │
            │                         │
            │  engines_supported:     │
            │    - airbyte            │
            │    - singer             │
            │    - native             │
            └────────┬────────────────┘
                     │
            ┌────────┴────────┐
            │                 │
            ▼                 ▼
    ┌──────────────┐   ┌──────────────┐
    │   Airbyte    │   │    Native    │
    │   Engine     │   │    Engine    │
    │              │   │              │
    │ • Docker     │   │ • In-process │
    │ • Isolated   │   │ • Python     │
    │ • JSONL      │   │ • Direct API │
    │   protocol   │   │              │
    │              │   │              │
    │ ✅ Implemented│  │ ✅ Implemented│
    └──────────────┘   └──────────────┘
            │
            ▼
    ┌──────────────┐   ┌──────────────┐
    │   Singer     │   │   Meltano    │
    │   Engine     │   │   Engine     │
    │              │   │              │
    │ • Subprocess │   │ • CLI-based  │
    │ • JSONL      │   │ • Singer     │
    │   messages   │   │   wrapper    │
    │              │   │              │
    │ ⚠️ Stub only │   │ ⚠️ Stub only │
    └──────────────┘   └──────────────┘

Runtime Selection:
  job.source.engine_override: "singer"  ← Override default
```

---

## 7. Secret Management Flow

```
                    dativo run --secret-manager vault
                              │
                              ▼
                    ┌──────────────────────┐
                    │  Secret Manager      │
                    │  Factory             │
                    └──────┬───────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌───────────────┐  ┌──────────────┐  ┌─────────────┐
│ Filesystem    │  │ Vault        │  │ AWS Secrets │
│               │  │              │  │ Manager     │
│ /secrets/     │  │ KV v1/v2     │  │             │
│   {tenant}/   │  │ AppRole auth │  │ IAM auth    │
│   stripe.env  │  │              │  │             │
└───────────────┘  └──────────────┘  └─────────────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                           ▼
                ┌─────────────────────┐
                │  Unified Secret Map │
                │                     │
                │  {                  │
                │    "stripe": {...}, │
                │    "postgres": {...}│
                │  }                  │
                └──────────┬──────────┘
                           │
                           ▼
                 ┌──────────────────┐
                 │ Runtime Injection│
                 │                  │
                 │ ${STRIPE_API_KEY}│
                 │         ↓        │
                 │   sk_live_***    │
                 └──────────────────┘
```

---

## 8. Proposed: Plugin SDK Architecture

```
┌────────────────────────────────────────────────────────────┐
│                   dativo-plugin-sdk                         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │              Base Classes & Mixins                    │  │
│  │                                                       │  │
│  │  • PaginatedAPIReader                                │  │
│  │  • OAuth2Mixin                                       │  │
│  │  • RateLimitMixin                                    │  │
│  │  • RetryMixin                                        │  │
│  │  • IncrementalMixin                                  │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                Helper Utilities                       │  │
│  │                                                       │  │
│  │  • parse_pagination()                                │  │
│  │  • handle_rate_limit()                               │  │
│  │  • refresh_oauth_token()                             │  │
│  │  • exponential_backoff()                             │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │               Testing Utilities                       │  │
│  │                                                       │  │
│  │  • MockSourceConfig                                  │  │
│  │  • RecordGenerator                                   │  │
│  │  • PluginTester                                      │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                CLI Tools                              │  │
│  │                                                       │  │
│  │  • dativo init connector --name my-api               │  │
│  │  • dativo test plugin --path my_reader.py            │  │
│  │  • dativo validate plugin --config job.yaml          │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘

Example Usage:

from dativo_sdk import PaginatedAPIReader, OAuth2Mixin

class MyReader(PaginatedAPIReader, OAuth2Mixin):
    def get_next_page_token(self, response):
        return response.get("next_page")
    
    # SDK handles:
    # - Pagination loop
    # - OAuth2 token refresh
    # - Rate limiting
    # - Retry logic
    # - Error handling
```

---

## 9. Current vs Proposed: Isolation Comparison

### Current (In-Process)

```
┌─────────────────────────────────────────────┐
│         Main Process (dativo)               │
│                                             │
│  ┌────────────────────────────────────┐   │
│  │  Orchestration Engine              │   │
│  └────────────────────────────────────┘   │
│                                             │
│  ┌────────────────────────────────────┐   │
│  │  Native Stripe Extractor           │   │
│  │  (full access to memory/secrets)   │   │
│  └────────────────────────────────────┘   │
│                                             │
│  ┌────────────────────────────────────┐   │
│  │  Custom Plugin (Python)            │   │
│  │  ⚠️ Can read /secrets/ directory   │   │
│  │  ⚠️ Can access all env vars        │   │
│  └────────────────────────────────────┘   │
│                                             │
│  ┌────────────────────────────────────┐   │
│  │  Credentials in Memory             │   │
│  │  STRIPE_KEY=sk_live_***            │   │
│  │  AWS_KEY=AKIA***                   │   │
│  └────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

### Proposed (Sandboxed)

```
┌─────────────────────────────────────────────┐
│         Main Process (dativo)               │
│                                             │
│  ┌────────────────────────────────────┐   │
│  │  Orchestration Engine              │   │
│  └────────────────────────────────────┘   │
│                                             │
│  ┌────────────────────────────────────┐   │
│  │  Plugin Sandbox Manager            │   │
│  │  • Launches Docker containers      │   │
│  │  • Injects secrets as env vars     │   │
│  │  • Enforces resource limits        │   │
│  └─────────┬──────────────────────────┘   │
└────────────┼────────────────────────────────┘
             │
             ├─────────────────┬─────────────┐
             │                 │             │
             ▼                 ▼             ▼
┌─────────────────┐  ┌─────────────┐  ┌─────────────┐
│ Container 1     │  │ Container 2 │  │ Container 3 │
│                 │  │             │  │             │
│ Stripe Plugin   │  │ Custom API  │  │ Rust CSV    │
│                 │  │   Plugin    │  │   Reader    │
│ ✅ Read-only FS │  │ ✅ 2GB RAM  │  │ ✅ 50% CPU  │
│ ✅ Only STRIPE  │  │ ✅ Network  │  │ ✅ No       │
│    _KEY env var │  │    isolated │  │    network  │
└─────────────────┘  └─────────────┘  └─────────────┘
```

---

## 10. Roadmap Timeline

```
Month 1: Security & Stability
├── Week 1-2: Plugin Sandboxing
│   ├── Docker-based execution
│   ├── Secret injection
│   └── Resource limits
└── Week 3-4: Plugin API Versioning
    ├── Version checks
    ├── Deprecation warnings
    └── Migration guide

Month 2: Ecosystem Enablement
├── Week 1-2: Singer/Meltano Extractors
│   ├── Subprocess execution
│   ├── Message parsing
│   └── State management
└── Week 3-6: Plugin SDK
    ├── Base classes & mixins
    ├── Helper utilities
    ├── Testing framework
    └── CLI scaffolding

Month 3: Performance & Observability
├── Week 1-2: Schema Discovery
│   ├── discover() interface
│   └── check() interface
├── Week 3-4: Parallelism
│   ├── ThreadPoolExecutor
│   └── Backpressure handling
└── Week 5-6: Observability
    ├── Prometheus metrics
    ├── OpenTelemetry tracing
    └── Dashboards

Beyond (6-12 months)
├── Plugin Marketplace
├── CDC Framework
├── State Backend Abstraction
└── Advanced Isolation (WASM)
```

---

## Legend

```
✅ = Implemented and working well
⚠️ = Partially implemented or needs improvement
❌ = Missing or not implemented
🤝 = Compatible/equivalent with industry standards
```

---

**Document Version:** 1.0  
**Last Updated:** November 27, 2025  
**Related Documents:**
- ARCHITECTURE_REVIEW.md (full technical analysis)
- ARCHITECTURE_REVIEW_SUMMARY.md (executive summary)

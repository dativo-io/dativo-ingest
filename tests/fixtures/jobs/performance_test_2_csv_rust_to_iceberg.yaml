# Performance Test 2: CSV Reader (Rust) -> Iceberg Table
# Tests: Rust CSV reader plugin writing to Iceberg table on S3 (MinIO)
tenant_id: perf_test_tenant
environment: test

source_connector: csv
source_connector_path: connectors/csv.yaml

target_connector: iceberg
target_connector_path: connectors/iceberg.yaml

asset: perf_test_data
asset_path: tests/fixtures/assets/csv/v1.0/perf_test_data.yaml

source:
  custom_reader: "examples/plugins/rust/target/release/libcsv_reader_plugin.so:create_reader"
  files:
    - path: "${PERF_TEST_CSV_FILE}"
      object: perf_test_data
  engine:
    options:
      batch_size: 100000
      delimiter: ","
      encoding: "utf-8"

target:
  connection:
    s3:
      endpoint: "${S3_ENDPOINT}"
      bucket: test-bucket
      access_key_id: "${AWS_ACCESS_KEY_ID}"
      secret_access_key: "${AWS_SECRET_ACCESS_KEY}"
      region: "${AWS_REGION}"
      path_style_access: true

catalog:
  type: nessie
  connection:
    uri: "${NESSIE_URI}"
  database: perf_test_db
  table_name: test2_csv_rust

schema_validation_mode: warn
logging:
  redaction: false
  level: INFO

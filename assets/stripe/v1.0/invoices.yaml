asset:
  name: stripe_invoices
  source_type: stripe
  object: invoices
  version: "1.0"
  
  schema:
    - name: id
      type: string
      required: true
    - name: customer
      type: string
      required: false
    - name: amount_due
      type: integer
      required: false
    - name: currency
      type: string
      required: false
    - name: status
      type: string
      required: false
    - name: created
      type: timestamp
      required: true
  
  governance:
    owner: data-team@company.com
    tags: [payments, invoices]
    classification: []
    retention_days: 365
  
  target:
    file_format: parquet
    partitioning: [ingest_date]
    mode: strict



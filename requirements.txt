# Dependencies for Dativo ingestion runner
# Note: Package version is defined in pyproject.toml
# This file is kept for Docker builds and should match pyproject.toml dependencies

# Core dependencies
pydantic>=2.0.0
pyyaml>=6.0

# Orchestration
dagster>=1.5.0

# Logging
python-json-logger>=2.0.0

# Schema validation
jsonschema>=4.0.0

# HTTP requests for infrastructure health checks
requests>=2.28.0

# Data processing
pandas>=2.0.0
pyarrow>=14.0.0

# Iceberg and Nessie
pyiceberg>=0.5.0
pynessie>=0.60.0

# Storage access
boto3>=1.28.0

# Cloud plugin execution (optional)
# Uncomment for AWS Lambda support:
# boto3>=1.28.0  # Already included above for S3 access
# Uncomment for GCP Cloud Functions support:
# google-cloud-functions>=1.13.0
# google-cloud-storage>=2.10.0

# Data catalog integration (optional)
# openmetadata-ingestion ~= 1.3.0  # Uncomment if using OpenMetadata catalog

# Testing (optional, for development)
pytest>=7.0.0
pytest-mock>=3.0.0

